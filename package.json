{
    "devDependencies": {
        "git-scripts": "^0.6.4",
        "surge": "^0.0.0"
    },
    "git": {
        "scripts": {
            "pre-push": "surge --project ./ --domain alan-wang-website.surge.sh"
        }
    }
}
